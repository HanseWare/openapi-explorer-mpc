[project]
name = "openapi-explorer-mcp"
version = "0.1.0"
description = "An MCP server that exposes OpenAPI definitions to LLMs via Server-Sent Events (SSE)."
requires-python = "==3.12"
dependencies = [
    "fastmcp>=3.1.1",
    "uvicorn>=0.39.0",
]
